name: Debug Data Collection - Simple Test

on:
  workflow_dispatch:

jobs:
  test-basic:
    name: Basic Test Job
    runs-on: ubuntu-latest
    timeout-minutes: 2
    
    steps:
    - name: Test Basic Functionality
      run: |
        echo "Testing basic workflow functionality"
        echo "Current date: $(date)"
        echo "GitHub ref: ${{ github.ref }}"
        echo "SUCCESS: Basic test completed"
